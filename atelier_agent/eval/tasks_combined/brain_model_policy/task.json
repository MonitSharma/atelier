{
  "id": "brain_model_policy",
  "category": "knowledge_to_model_policy",
  "difficulty": "easy",
  "edit_scope": "single_line",
  "expected_tools": ["search_notes", "repo_map", "read_file", "edit_file", "test_runner"],
  "corpus": ["Project.md", "README.md", "docs/ARCHITECTURE.md"],
  "prompt": "This is a combined knowledge-to-build task in `{path}`. First use search_notes to find which local model is the default brain model for Atelier. Then use repo_map and read_file with the workspace-relative paths shown by repo_map, update the code to match that project decision, and run test_runner on `{path}`. Finish only when tests pass cleanly.",
  "verify_path": "."
}

{
  "id": "locality_constraint",
  "category": "knowledge_to_policy",
  "difficulty": "easy",
  "edit_scope": "single_line",
  "expected_tools": ["search_notes", "repo_map", "read_file", "edit_file", "test_runner"],
  "corpus": ["Project.md", "README.md", "myNotes.md", "docs/ARCHITECTURE.md"],
  "prompt": "This is a combined knowledge-to-build task in `{path}`. First use search_notes to find whether Atelier allows cloud APIs under the hard project constraints. Then use repo_map and read_file with the workspace-relative paths shown by repo_map, update the code to match that constraint, and run test_runner on `{path}`. Finish only when tests pass cleanly.",
  "verify_path": "."
}
